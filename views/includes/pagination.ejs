<section class="pagination">
  <% for(let i = 1; i <= lastPage; i++){%>
  <a href="?page=<%= i%>" class="<%= currentPage == i ? 'active' : '' %>"
    ><%= i%>
  </a>
  <%}%>
</section>
<!-- <section class="pagination">
  <% if((currentPage !== 1 && previousPage !== 1)|| currentPage === 1){%>
  <a href="?page=1">1</a>
  <% } %> <% if(hasPreviousPage){%>...<a href="?page=<%= previousPage%>"
    ><%= previousPage%></a
  >
  <a href="?page=<%= currentPage %>" class="active"><%= currentPage %></a>
  <% } %> <% if(hasNextPage){%>
  <a href="?page=<%= nextPage%>"><%= nextPage %></a><% } %> <% if(lastPage !==
  currentPage && nextPage !== lastPage){%>...<a href="?page=<%= lastPage%>"
    ><%= lastPage%></a
  >
  <% } %>
</section> -->
